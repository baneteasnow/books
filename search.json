---
layout: null
---
[
  {% for item in site.data.list001 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list002 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list003 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list004 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list005 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list006 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list007 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list008 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list009 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
    {% for item in site.data.list010 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
      {% for item in site.data.list011 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
      {% for item in site.data.list012 %}
    {

      "title"     : "{{ item.title }}",
      "url"       : "{{ site.baseurl }}bookstoc#{{ item.title }}",
      "author"    : "{{ item.author }}",
      "year"      : "{{ item.year }}",
      "no"        : "{{ item.No }}",
      "bookshelf" : "{{ item.notes }}",
      "uniqueid"  : "{{ item.bookshelf }}"

    } {% unless forloop.last %},{% endunless %}
  {% endfor %},
]